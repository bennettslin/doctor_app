<%= bootstrap_form_for @paper do |f| %>
  <%= f.text_field :title, placeholder: "Paper title" %>
  <%= f.text_area :body, placeholder: "Paper body" %>
  <%= f.collection_check_boxes :tag_ids, @tags, :id, :name, label: "Tags" %>
  <%= f.primary %>
<% end %>